# Working agreement

- Never run git commands. No staging, committing, or pushing. Tomer handles all git.
- Standard library plus pytest only. No new dependencies without asking.
- Implement only what the current prompt asks for. Don't anticipate later stages.
- Ask before creating files outside the paths named in the prompt.
